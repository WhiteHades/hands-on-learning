# Print a Greeting

The starter program compiles, but it prints the wrong greeting.

Open `main.c` with `e` or Enter. Change the program so its complete output is:

```text
Hello, learner!
```

Use `Space r` to compile and run it. Use `Space t` to check the exact output.
The check ignores only trailing newline characters.
